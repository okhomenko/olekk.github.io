{
  "scripts": {
    "build": "npm run build:site && npm run seo",
    "build:site": "eleventy",
    "seo": "node scripts/generate-seo.mjs _site",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
